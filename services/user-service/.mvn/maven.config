-Dkeycloak.realm=${KEYCLOAK_REALM} \
-Dkeycloak.auth-server-url=${KEYCLOAK_AUTH_SERVER_URL} \
-Dkeycloak.resource=${KEYCLOAK_RESOURCE} \
-Dkeycloak.credentials.secret=${KEYCLOAK_CREDENTIALS_SECRET} \
-Dkeycloak.token.endpoint=${KEYCLOAK_TOKEN_ENDPOINT}
